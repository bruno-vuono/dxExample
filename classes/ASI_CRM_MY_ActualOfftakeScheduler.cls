global class ASI_CRM_MY_ActualOfftakeScheduler implements Schedulable {
    global void execute(SchedulableContext SC) {
        //batchable b = new batchable(); 
        //database.executebatch(b);
    }
}